name: CI

on:
  # PR builds moved to pr-build.yml, which sandboxes untrusted PR code under landrun in a
  # trusted (base-defined) workflow. This run is only the post-merge health check on main.
  push:
    branches: [main]

permissions:
  contents: read

# Keep the cache publisher live under a burst of main pushes. GitHub retains at
# most one running and one pending run in this group: the running build is never
# cancelled before it can publish, while superseded pending commits do not each
# start an expensive cache-miss rebuild. In a burst, this deliberately coalesces
# intermediate commits' post-merge checks too; the newest pending tip rechecks
# the entire tree, though localizing a failure may then require bisection. No
# merge decision consumes this post-merge workflow's per-commit conclusion.
concurrency:
  group: main-ci-cache-publisher
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Build
        uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # v1
        with:
          build: false
          test: false
          lint: false
          use-mathlib-cache: true
          use-github-cache: false

      # Axiom audit (replaces the old textual no-sorry grep). Inspects the built
      # KIP126 environment and rejects any axiom outside {propext, Classical.choice,
      # Quot.sound}, catching sorry/admit (sorryAx), native_decide (Lean.ofReduceBool),
      # and home-rolled axioms, including ones reaching in through imports.
      - name: Axiom audit (KIP126/ uses only allowlisted axioms)
        run: |
          lake build --iofail
          lake exe axioms

      - name: Run source inventory, unit, and Regression gates
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/check_source_inventory.py --lean-timeout 900
          python3 -m unittest discover -s scripts -p 'test_*.py' -v
          lake build \
            KIP126.Classical.Adams.Regression \
            KIP126.Classical.ExtensionSS.Regression \
            KIP126.Comparison.ClassicalSynthetic.Regression \
            KIP126.Core.SpectralSequence.ConvergenceRegression \
            KIP126.Core.SpectralSequence.SpectralObjectAdapterRegression \
            KIP126.External.ClaimsRegression \
            KIP126.External.ProvenanceRegression \
            KIP126.External.SourceInventoryRegression
          git diff --check

      - name: Run trusted infrastructure unit tests
        run: |
          PYTHONPATH=. python3 scripts/perf/test_perf.py -v
          PYTHONPATH=. python3 scripts/test_status_projection.py
          python3 -m py_compile .github/euler/status_projection.py scripts/perf/*.py scripts/profile/*.py

      # --- Lake artifact-cache publish (dormant until configured) ---------------------
      # Publish KIP126's OWN oleans (root-package only) to the Lake cache, so pr-build can
      # fetch them and recompile only a PR's changed modules instead of the whole library.
      # Runs only once the upload credential + S3 endpoints are installed; until then these
      # two steps are skipped and main's health check is unchanged. Mathlib's oleans are
      # never uploaded: `-o` records only root-package outputs, and Mathlib stays on
      # `lake exe cache get`. See pr-build.yml for the consuming side.
      - name: Configure the Lake cache upload (no-op unless secret + S3 endpoints are set)
        id: cachecfg
        env:
          LAKE_CACHE_KEY_RAW: ${{ secrets.LAKE_CACHE_KEY }}
          S3_ARTIFACT_ENDPOINT: ${{ vars.LAKE_CACHE_ARTIFACT_ENDPOINT }}
          S3_REVISION_ENDPOINT: ${{ vars.LAKE_CACHE_REVISION_ENDPOINT }}
        run: |
          if [ -n "$LAKE_CACHE_KEY_RAW" ] && [ -n "$S3_ARTIFACT_ENDPOINT" ] && [ -n "$S3_REVISION_ENDPOINT" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "::notice::Lake cache upload configured — publishing KIP126's oleans"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Lake cache upload not configured — skipping (set the LAKE_CACHE_KEY secret and the LAKE_CACHE_ARTIFACT_ENDPOINT / LAKE_CACHE_REVISION_ENDPOINT repo variables to enable)"
          fi

      - name: Publish KIP126's oleans to the Lake cache
        if: ${{ steps.cachecfg.outputs.enabled == 'true' }}
        env:
          LAKE_ARTIFACT_CACHE: "true"
          LAKE_RESTORE_ARTIFACTS: "true"
          # Don't download during the packing build — we only want to PUT, not GET. (The
          # S3 host needs SigV4 even for reads, so an anonymous GET here would 403 anyway.)
          LAKE_NO_CACHE: "true"
          LAKE_CACHE_DIR: ${{ github.workspace }}/.lake/cache
          # Passed under non-LAKE_ names so Lake never sees the deprecated endpoint
          # environment variables; we hand it the endpoints through a `--service` config.
          S3_ARTIFACT_ENDPOINT: ${{ vars.LAKE_CACHE_ARTIFACT_ENDPOINT }}
          S3_REVISION_ENDPOINT: ${{ vars.LAKE_CACHE_REVISION_ENDPOINT }}
          LAKE_CACHE_KEY_RAW: ${{ secrets.LAKE_CACHE_KEY }}
        run: |
          export PATH="$HOME/.elan/bin:$PATH"
          # curl --user wants "<ACCESS_KEY_ID>:<SECRET>"; trim trailing whitespace and mask
          # it so it never lands in the log. LAKE_CACHE_KEY (the upload secret) stays an
          # env var; only the endpoints move to the --service config below.
          KEY="$(printf %s "$LAKE_CACHE_KEY_RAW" | sed -e 's/[[:space:]]*$//')"
          echo "::add-mask::$KEY"; export LAKE_CACHE_KEY="$KEY"
          # Define the authenticated S3 service in a Lake system config and select it with
          # `--service` (the env-var form of endpoint config is deprecated).
          CFG="$RUNNER_TEMP/lake-cache.toml"
          cat > "$CFG" <<TOML
          cache.defaultUploadService = "kip126-r2"
          [[cache.service]]
          name = "kip126-r2"
          kind = "s3"
          artifactEndpoint = "$S3_ARTIFACT_ENDPOINT"
          revisionEndpoint = "$S3_REVISION_ENDPOINT"
          TOML
          export LAKE_CONFIG="$CFG"
          # Pack the already-built oleans into the local Lake cache (no recompile — the
          # health-check build above left matching traces), emit the root-package
          # input->output mappings, then upload just those artifacts (SigV4 PUT to S3).
          # `lake cache put` configures the workspace, so the revision (this main commit)
          # and the toolchain/platform scope are detected automatically and match what
          # pr-build's `lake cache get --service ... --repo` derives.
          lake build >/dev/null
          lake build --no-build -o .lake/outputs.jsonl
          echo "root-package mapping entries: $(wc -l < .lake/outputs.jsonl)"
          lake cache put .lake/outputs.jsonl --service kip126-r2 --repo surenny/KIP126
