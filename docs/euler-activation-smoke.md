This file is a temporary, validation-only marker for the KIP126 activation gate.

It exercises the allowlisted documentation overlay on a non-initializer pull request.
The pull request is never merged and is closed after exact-head CI, rerun, and
semantic-review fail-closed behavior are observed.
