\chapter{Source coverage and audit index}
\label{chap:coverage-audit}

This chapter is a navigation and audit record, not an additional mathematical
input.  It maps the source-of-record snapshot to the chapters above and makes
the known source repairs visible.  Mathematical claims still live in their
individual nodes, with their own direct dependencies and status markers.

\section{Section-by-section coverage}

The introduction at \texttt{aimpaper/main.tex:78--252} is represented by the
classical Adams interfaces in Chapter~\ref{chap:classical-adams}, the precise
low-line literature statements in Chapter~\ref{chap:external-results}, the
permanent-cycle reduction in Chapter~\ref{chap:near126}, and the geometric
endpoints in Chapter~\ref{chap:final-kervaire}.  The two open questions live
in the Kervaire-setup chapter and do not feed the proof graph.

Section 2, lines 253--669, is represented in
Chapter~\ref{chap:extension-ss}: the filtered two-term construction,
essential and inessential extensions, crossings, all comparison corollaries,
and the exact-at-the-middle theorem are separate nodes.  The concrete
$\eta$-regression and the factorization consumer occur later in the internal
comparison chapter, after their explicit inputs.

Section 3, lines 670--997, is split by role.  The stable and synthetic object
chapters define the $H\mathbb F_2$-synthetic context, $\nu$, $\lambda$,
quotients, normalized-map data, and synthetic Adams objects.  Pstragowski and
BHS statements occur only in Chapter~\ref{chap:external-results}; rigidity,
$E_\infty$ formulas, lifted-triangle consumers, and normalized-triangle proofs
occur in Chapter~\ref{chap:comparison-and-rules}.

Section 4, lines 998--1272, uses the synthetic-extension chapter for the
weightwise ESS, the $\lambda$--$\rho$--$\delta$ triangle, and solution-tower
definitions.  Located completeness is an external result; finite and infinite
target formulas, quotient compatibility, and classical-crossing comparison
are internal deductions in Chapter~\ref{chap:comparison-and-rules}.

Section 5, lines 1273--1601, is represented in the classical-page extension
chapter.  The $\widehat f_{r-1}$ page reduction, exact indeterminacy, crossing
definition, and finite/infinite clauses are defined there.  Crossing
comparison, $\lambda$-inversion consumers, and the load-bearing Hopf-map
regressions occur later in the internal and computed-input chapters.

Section 6, lines 1602--2101, is represented in
Chapter~\ref{chap:comparison-and-rules}: the generalized Leibniz rule, the
generalized Mahowald trick, the page-loss theorem, and stretching to later
pages are separate project obligations.  May's two-triangle theorem itself is
listed earlier as a located external result.

Section 7, lines 2102--2782, is represented in
the flat setup, external-results, computed-results, and near-$126$ chapters.
Their graph separates BJM/BX literature, typed high-stem evidence,
choice-independence, the unique possible $d_{12}$,
Toda/two-extension arguments, the Hopf-cofiber obstruction, and the final
contradiction.  The source's circular-looking use at line 2496 is repaired by
first proving the filtration-leading-term Lemma
\ref{lem:toda-h0-leading-nonvanishing}; only after the Toda candidate
enumeration does Corollary~\ref{cor:h02x1259-d5-zero} deduce the unresolved
$d_5$ is zero.

The permanent-cycle theorem and both geometric endpoints are universally
quantified over the single explicit record of
Definition~\ref{def:kervaire-main-input}.  Its literature, computation, and
geometric subrecords expose every trust-boundary value as a named projection;
every data-dependent near-$126$ lemma is parameterized by the same computation
projection, and the graph contains no implicit global package of those inputs.

The appendix at lines 2783--3290 and Table 1 in Section 7 use two flat
chapters.  The computation-schema chapter defines the row, expression,
catalogue, and bundle types without asserting any value.  Chapter
\ref{chap:computed-inputs} then contains the twelve datasets---401 nonempty rows
and nine zero bands---together with all 49 CW spectra and their $E_2$ pages,
all 180 maps, initial $d_2$ ranges, and imported propagated outputs.  Its
catalogue-completeness theorem links the machine records to the appendix rows.
The active part of \texttt{aimpaper/112.tex} defines Figure 9 only;
Definition~\ref{def:near126-figure-contract} treats it as a derived
visualization, while its commented duplicate block is excluded.

\section{Recorded source repairs}

The Blueprint deliberately does not copy the following source defects:
\begin{enumerate}
  \item line 146's Hopf-survival inequality is read as $j\le3$, consistently
    with the following nonzero $d_2$ formula for $j\ge4$;
  \item line 604's class $z$ lies in $E_\infty(Z)$, not $E_\infty(Y)$;
  \item line 819's Bockstein variable is $\lambda$, not $\tau$;
  \item Proposition 4.6 separates the infinite target from its image in a
    finite $\lambda$-quotient;
  \item line 1522 uses $\widehat f_{r-1}$, consistent with Definition 5.1;
  \item Proposition 6.20 receives typed ranges for its correction classes;
  \item line 2496's backwards dependence is replaced by a noncircular
    filtration-leading-term argument;
  \item the lift from modulo $\lambda^3$ to modulo $\lambda^5$ in Lemma 7.20
    requires the independent obstruction-vanishing evidence node.
  \item the mapping-cone construction remains triangulated until an explicit
    homological-functor bridge constructs the abelian spectral object;
  \item the displayed AIM page is
    $E_r\cong Z_{r-1}/B_{r-1}$; the $Z_r/B_r$ quotient appears only after
    taking the displayed $d_r$ homology, and filtered $E_0$ indices are moved
    through an explicit reindexing;
  \item arbitrary Adams spectral sequences receive external pairings, while
    internal multiplication is asserted only from explicit ring-object data;
  \item the finite-quotient formulas use exponents at least two, and
    $\lambda$-quotient maps retain their suspension and weight shift;
  \item the untruncated maps
    $\lambda^n:\Sigma^{0,-n}\nu X\to\nu X$ and
    $\rho:\nu X\to\nu X/\lambda^n$ have separate boundary-quotient and
    cycle-inclusion formulas, so the infinite $\delta$ route does not
    extrapolate a finite-endpoint theorem;
  \item Mahowald survival is deduced only from the compatible lift produced by
    May's diagram, not from a false converse for a preselected representative;
  \item page stretching uses a first-obstruction theorem and, at the infinite
    page, a coherent inverse-limit argument rather than bare objectwise
    completeness;
  \item the Hopf finite crossing/loss certificate is stated without assuming
    the $E_\infty$ relation; it proves stretching first, and only then is used
    to certify no crossing for the resulting relation;
  \item the BJM--BX source choice is kept distinguished until project-level
    choice independence transports its criterion to every order-two choice,
    with the finite criterion quantified explicitly over every $r\ge1$;
  \item the normalized Hopf cofiber is $C([h_2])$, using the directly located
    identity $\widehat\nu=[h_2]$; $C(\lambda[h_2])$ is only a comparison
    source.  Its proof first builds the zero-tagged exactness datum and one
    chosen lifted triangle required by the normalized-cofiber interface.
\end{enumerate}

\section{Status boundary}

Only exact declarations already compiled in the repository carry
\texttt{leanok}; only declaration names checked against the pinned Mathlib
revision carry \texttt{mathlibok}.  Every paper theorem, literature adapter,
data transcription, and missing semantic interface remains
\texttt{notready}.  Thus this document describes a complete formalization
plan and its proof frontier, not a completed Lean proof of the paper.
