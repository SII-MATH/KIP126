# Codex 工作目录约束记录

## 明确约束

本项目当前的唯一工作目录是：

```text
/home/wang/KIP126/KIPBase
```

后续所有查看、搜索、编译、修改和生成文件的操作，都必须限定在这个目录及其子目录内。

## 本次错误

之前多次使用 `/home/wang/KIP126` 作为工作目录，超出了用户指定的 `KIP126/KIPBase` 范围。即使操作目标文件位于 `KIPBase` 内，这种工作目录设置仍然违反了边界要求，也导致检查结果混入父项目的配置和构建状态。

## 后续执行规则

1. 所有命令的工作目录固定为 `/home/wang/KIP126/KIPBase`。
2. 不搜索、不读取、不修改父目录 `/home/wang/KIP126` 中的文件。
3. 构建时只使用 `KIPBase` 自己的 `lakefile.toml`、`lake-manifest.json` 和 `lean-toolchain`。
4. 如果任务必须依赖父目录文件，先停止并向用户说明，不擅自越界。
5. 汇报编译结果时明确区分 `KIPBase` 自身问题和父项目问题，不再使用父项目结果代替本项目结果。

## 额外反省

在 AF 的命题化处理中，我错误地把 `AF_eq` 写成“存在一个整数值等于 `k`”，并进一步讨论了不需要的唯一性。正确要求是：AF 不定义成函数，`AF_eq` 应直接由下界命题 `AF_ge` 表达。后续只按用户指定的数学命题工作，不自行扩展接口。

我还在用户要求暂缓 Steenrod 代数部分后继续围绕其接口展开。后续遇到明确要求“注释掉”的部分，直接停止使用该部分，不继续修补其公理接口。
